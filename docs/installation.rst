============
Installation
============

At the command line::

    $ easy_install django-envelope-save

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-envelope-save
    $ pip install django-envelope-save
